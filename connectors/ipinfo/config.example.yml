# IPinfo connector configuration
# Copy to config.yml and fill in your values.

name: ipinfo
mode: auto              # manual | auto | both
connector_type: enricher
scope:
  - ipv4-addr
  - ipv6-addr

ipinfo_token: "REPLACE_ME"
