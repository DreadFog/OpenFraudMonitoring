{
  "name": "SELENIUM_SAMPLE",
  "description": "Sample custom rule — triggers when FPScanner detects Selenium properties",
  "rule_type": "realtime",
  "logic": "AND",
  "conditions": [{"field": "det_has_selenium_property", "op": "eq", "value": "true"}],
  "score_modifier": 1
}
