{
  "name": "ofm-client",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "build:dev": "FPSCANNER_KEY=dev-key vite build",
    "build:prod": "vite build",
    "watch": "FPSCANNER_KEY=dev-key vite build --watch"
  },
  "dependencies": {
    "fpscanner": "file:../fpscanner"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
